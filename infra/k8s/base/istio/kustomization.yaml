apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# NOTE: istio-base.yaml is applied via `istioctl install`, not kustomize.
# Only the mesh policy resources are managed here.
resources:
  - peer-authentication.yaml
  - destination-rules.yaml
  - authorization-policies.yaml
  - virtual-services.yaml
